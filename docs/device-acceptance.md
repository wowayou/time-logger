# 真机验收单（v82 – v86）

> 用途：在**真实数据的真机**上验收标签生命周期（v82/v83）、反馈批次（v84）与标签合并（v85）。
> 判据来源：`docs/decisions.md` D19 / D20 / D21 与 `CLAUDE.md` 对应版本行。
> 用法：从上往下点，任何一步与「期望」不符就停下记录（截图 + 步骤号 + 版本号 + 当时语言）。
> 状态：v82–v86 各跑过一轮。**v86 已验收（2026-08-05）：F1 提示可见性 pass、F3 时钟自愈 pass、F2 白边结案为系统行为**。仍未测：D 节（标签合并，缺合适的记录）。

## 0 · 前置（2 分钟）

| # | 操作 | 期望 |
|---|---|---|
| 0.1 | 「···」→ **备份与导入** → **存储备份**，存一份 JSON | **iPhone/iPad**：系统文件面板弹出，可选目录。**Android/桌面**：直接下载到「下载」目录，**不会**让你选目录——这是设计（`showSaveFilePicker` 在 Android Chrome 上不可用）。想自己挑位置就用同一页的「**分享备份**」→ 系统分享面板 → 存到文件 |
| 0.2 | 冷启动或切回前台，等更新横幅 → 点「更新应用」 | 加载新版；**本机记录不变** |
| 0.3 | 「···」滑到底看版本号 | 显示 **时间尺 v86**（若仍是旧版，走「···」→ 高级 → 修复更新通道） |

> 这些版本只写 `timelog.config`（标签配置），记录数据只在你点「合并」时被改写标签名。0.1 仍然要做——真机上是唯一一份真实数据。

---

## F · v86 本轮重点（**先跑这一节**）

> v86 修的三条都只有真机能判，所以它们排在最前面。跑完这三条，其余章节按需抽查。

### F1 报错提示可见性（v84 回归的修复）

| # | 操作 | 期望 |
|---|---|---|
| F1.1 | 标签设置里，把**靠上**那一行名字清空 → 保存 | 视图滚到那一行 **且** 红色提示条粘在正文底边、就在屏幕上（v84 只做到了前半句，后半句留在了屏幕外——真机上表现为「点了没反应」） |
| F1.2 | 新建一行填 `未知` → 保存 | 同上：提示条可见，写着「保留名」 |
| F1.3 | 新建一行填一个已有标签名 → 保存 | 同上：提示条可见，写着「重复」 |

### F2 退出动效白边 —— **已结案：系统行为，不做**（2026-08-05）

维护者做完三步分诊：**版本是 v86**、**滚动时右缘没有滚动条**（v86 的 CSS 确实生效了）、
**其他 PWA 做同样的退出手势也有白边**。第三条是对照实验，一次性把责任分清了：

> 白边来自 Chrome/One UI 在窗口缩放动效期间用**窗口底色**填充未绘制区域，**Web 侧改不掉**。
> 它不是我们画的，也不是滚动条占位。

本条**关闭**，不再排期。若将来出现新证据（例如别的 PWA 明显更轻、或换 Android 版本后消失），
再开。相关的两处改动去留见「白边追查记录」表末。

### F3 前台时钟自愈

| # | 操作 | 期望 |
|---|---|---|
| C6.1 | 把应用切后台几分钟再切回来 | hero 的「截至 hh:mm」与时间轴的「现在 hh:mm」立即是当前时间 |
| C6.2 | 应用一直留在前台不动几分钟，然后**点一下页面** | 时间立即刷新到当前分钟（v86：进程被系统冻住时定时器不会自己醒，碰一下就自愈） |


---

## A · v82 删除

入口：「···」→ **标签高级设置**

| # | 操作 | 期望 |
|---|---|---|
| A1 | 看常用标签行 | 第二行右侧是「**N 条记录**」，**没有**删除入口 |
| A2 | 看没用过的标签行 | 第二行右侧是红色「**删除**」 |
| A3 | 点一次「删除」 | 该行变灰、名字划线、输入框禁用，按钮变「撤销」；其它行不受影响 |
| A4 | 点「撤销」 | 行恢复正常 |
| A5 | 再点「删除」→ 右上「**保存**」 | 回到「更多」；重开设置页，那一行已消失 |
| A6 | 找个**有记录**的标签，清空名字 → 保存 | 报错「标签名不能为空…」**且提示条就在屏幕上**（v86 修：此前提示挂在正文末尾、留在屏幕外，看起来像「没反应」），且没有任何东西被删掉 |
| A7 | （可选）把某组零记录标签全删 → 保存 → 重开 | 该组显示空态提示，下面仍有「＋ 新建标签」 |

## B · v83 新建

| # | 操作 | 期望 |
|---|---|---|
| B1 | 「偏航标签」组底部点「**＋ 新建标签**」 | 组内多出一空白行，输入框自动聚焦、键盘弹出；左缘竖脊是偏航色 |
| B2 | 输入名字 → 保存 → 「记一条」→ 切到「偏航」桶 | 新标签出现在 chip 里可选 |
| B3 | 「主线」组新建一个 → 保存 → 重开设置页 | 新主线名排在**最后**；第一行仍带「**当前**」徽章 |
| B4 | 点「＋ 新建标签」但什么都不写 → 保存 | 不报错、也不多出任何标签 |
| B5 | 新建行填**已有标签同名** → 保存 | 报错「「X」重复了…」**且提示条可见**（**不是**合并提示——草稿行没落过库） |
| B6 | 新建行填 **未知** → 保存 | 报错「「未知」是未记录桶的保留名…」**且提示条可见** |
| B7 | 新建行写点字 → 点右侧「**移除**」 | 该行直接消失 |

---

## C · v84 反馈批次（提示可见性部分已并入 F1）

### C2 撤销横幅（方案 b）

| # | 操作 | 期望 |
|---|---|---|
| C2.1 | 删一条记录 → 横幅出现后**什么都不做** | 横幅约 8 秒后自行消失（上限未改） |
| C2.2 | 再删一条 → 横幅出现后**滚动列表或点任意处** | 横幅**立即收起**，撤销随之失效（记录保持已删） |
| C2.3 | 再删一条 → 直接点横幅上的「**撤销**」 | 记录恢复——点撤销本身不能被当成「你在做别的事」 |

### C3 「更多」瘦身与二级页

| # | 操作 | 期望 |
|---|---|---|
| C3.1 | 打开「···」 | 共 **9 行**：摘要 / 备份与导入 / 标签高级设置 / 阶段格言 / 主题 / 语言 / 说明 / 隐私政策 / 高级 |
| C3.2 | 进「备份与导入」，点「关闭备份与导入」 | 回到「更多」，不是整层关掉 |
| C3.3 | 「更多 → 备份与导入 → 导入备份」，选一个**坏文件**，点「关闭导入检查」 | 回到「**备份与导入**」（两层返回栈）；再关一次回到「更多」 |
| C3.4 | 进「高级」，开关一次「启动诊断」 | 停在「高级」页，**不会**被弹回「更多」；开启后多出「复制启动诊断样本」 |
| C3.5 | 二级页上从抓手条下拉 | 可下拉关闭，回到上一层 |

### C4 时长文案

| # | 操作 | 期望 |
|---|---|---|
| C4.1 | 看 hero 大数字与时间轴时长 | 中文界面下**没有 `~`**（如 `1h44min`、`50min`）；英文界面仍是 `1h 44m` |

---

## D · v85 标签合并与大小写

> 这一节会**改写记录的标签名**（只在你点「合并」之后）。先确认 0.1 的备份已存好。

| # | 操作 | 期望 |
|---|---|---|
| D1 | 标签设置里把 `Sleep` 改名成 `睡觉`（或你那对中英同义标签）→ 保存 | 出现提示「「Sleep」的 N 条记录会归到「睡觉」，「Sleep」这一行消失」+ 一个「**合并**」按钮；**此时还没写入** |
| D2 | 点「合并」 | 源标签行消失；目标标签**保留自己的桶与超长段免确认**；那 N 条记录改归目标标签 |
| D3 | 回日视图看统计 | 合并前后**总时长不变**，只是归到了同一个标签下 |
| D4 | 如果配置里同时有 `sleep` 和 `Sleep`：直接进设置页点保存 | 直接给出合并提示（用得多的那种拼写留下），不再是死路 |
| D5 | 记一条，自定义标签敲 `SLEEP`（配置里是 `Sleep`） | **不新增标签**；时间轴上显示 `#Sleep`（存的是配置里的权威拼写） |
| D6 | 把你那批英文默认标签（`Meals`/`Wash up`/`Commute`…）逐个并进中文对应标签 | 每次都是「提示 → 合并」两步；并完后设置页只剩一套 |

> **边界**：`睡觉` ≡ `Sleep` 这种**跨语言**等价要手动合并一次——应用不维护同义词表（D21）。

---

## E · 通用（每轮都看）

| # | 操作 | 期望 |
|---|---|---|
| E1 | 新建/编辑弹出键盘时看面板 | 头部「取消 / 标题 / 保存」始终在顶；键盘开合面板几何不动（v43 红线） |
| E2 | 标签很多时上下滚设置页 | 分组不被拦腰裁行（P21/P34）；末行「＋ 新建标签」完整可点 |
| E3 | 横竖屏切换 | 布局自适应，不横向溢出 |
| E4 | 切英文界面抽查 | 控件文案为英文、桶名 Focus/Upkeep/Drift；**标签名一个字都不变** |
| E5 | 收尾 | 删掉测试用标签；日视图统计与验收前一致（除非你在 D 节做过合并） |

---

## 白边追查记录（四轮 + 结案）

| 轮次 | 假设 | 证据 | 结论 |
|---|---|---|---|
| 1（v84 前） | 亮色页面 vs 深色 `background_color` 错配 | 首段录屏只有亮色 | 维护者**暗色对照实验证伪**（暗色下白边仍在） |
| 2（v84） | `color-scheme` 从未声明 → UA 按 light 画自己的表面（含合成器基础背景色） | 代码核查：全项目无 `color-scheme` | 已补四处声明。**它管的是颜色，不是占位**——保留，但不是这条白边的解 |
| 3（v85/v86） | **经典滚动条占的轨道** | 第二、三次录屏逐帧放大：那条通高浅色带**内部有滚动条滑块**；`html { scrollbar-gutter: stable }` 还把它恒定预留 | v86 在 `max-width: 719px` 下 `scrollbar-gutter: auto` + `scrollbar-width: none` + `::-webkit-scrollbar{width:0}` |
| 4（2026-08-05 新录屏） | 上一条**可能站不住** | 同一套取样：两段录屏在**静止态**下左右边缘亮度对称（220/220），白边**只在缩放动效期间**出现——与「恒定占位」不符。滑块只在滚动时淡入，退出手势本身就带出一次滚动，故「动效里看见滑块」不能证明它恒定占位 | 第 3 轮结论下早了：那条证据只够说明「动效期间滚动条可见」 |
| **结案（2026-08-05）** | **系统行为** | **对照实验**：维护者在**其他 PWA** 上做同样的退出手势，**白边同样出现**；同时确认在跑 v86、且滚动时右缘已无滚动条 | Chrome/One UI 在窗口缩放期间用**窗口底色**填充未绘制区域，**Web 侧改不掉**。本条关闭，不做 |

**方法论教训（写给下一个接手的人，也写给我自己）**：这条追查花了四轮，前三轮全在**我们自己的 CSS 里找嫌疑人**，一次对照组都没做。而「换个应用做同样动作」只要 20 秒，一次就分清了责任。`docs/HANDOFF.md` 的验收纪律里本来就有「归因必须有对照组」——我知道这条，却没在第一轮用。**下次遇到「只有在某个系统动效里才出现」的现象，第一步就是跨应用对照，不是读自己的 CSS。**

**两处改动的去留**：

| 改动 | 来源假设 | 现状 |
|---|---|---|
| v84 `color-scheme` 四处声明 | 假设 2（UA 按 light 画自己的表面） | **保留**，且理由独立成立：暗色下滚动条、原生控件、文本选中都该跟着主题走 |
| v86 窄屏隐藏根滚动条 | 假设 3（滚动条占位）——**已被证伪** | **保留但换理由**：手机上恒定预留一条滚动条轨道本身就不对（iOS 与多数 Android 都是 overlay）。它**不再是白边的解药**，别在文档或提交信息里那样引用它。若维护者认为丢了滚动位置指示更亏，随时可以回退这几行 CSS |

**本地复现不了**：headless Chromium 一律 overlay 滚动条，`--disable-features=OverlayScrollbar` 实测切不过来（`clientWidth === innerWidth` 恒成立），所以自动化只能锁**声明**（`tests/v86_fixes.spec.js`），视觉判断只有 S23 能给。

## 状态栏那条线（2026-08-04 截图，判为系统侧）

维护者圈出的位置取样：线上方 235.5、线本身 **194**、线下方 238（Y 亮度）。也就是说它是一条**比两侧都暗**的 1px 硬线。而本项目在那一带只画两样东西：`.statusbar-scrim` 的 `--bg` 实色，和它 `::after` 的 `--bg → transparent` 渐变——**没有任何比 `--bg` 更暗的东西**，画不出这条线。判为系统侧（One UI 状态栏分隔线／Android 边到边 scrim）。

可证伪的对照实验（10 秒）：切暗色再看一次。若线仍在、且仍比两侧暗，就是系统画的；若它跟着主题翻转或消失，那才是我们的，回来再查。

## 第二轮反馈（2026-08-04，针对 v84/v85）

| 步骤 | 结果 |
|---|---|
| 0.1 | Android 不给选目录 —— **不是缺陷**，期望文案已改对（要自选位置用「分享备份」） |
| A6 / B5 / B6 | 拦下了但**看不到提示** —— **v86 已修**（提示条粘正文底边；用例判据换成「在视口里」） |
| C1.1 | 滚到出错行✓，但同样没提示 —— 同上 |
| D 节（合并） | 未测（没有合适的记录；上一台是繁体设备） |
| 其余 | 通过 |
| 新报 | 状态栏多出一条线（见上，判为系统侧）；前台时钟停在一两分钟前（**v86 已加触摸自愈**）；白边仍在（**v86 第三轮修法**） |

## 第一轮反馈（2026-08-04，针对 v82/v83）

验收结论「基本可以」，8 条调整项的裁决见 `docs/decisions.md` D20/D21：

1. 空名报错后视图自动滚到最底部 —— **v84 已修**。
2. 中英文标签并存、`sleep` 与 `Sleep` 视为两个 —— **v85 已做**（大小写同一性 + 合并出口，D21）。
3. 标签管理多选 —— **不做**（D20）。
4. 撤销横幅停留过久 —— **v84 已改**（方案 b）。
5. 「更多」面板过长 —— **v84 已改**（13 行 → 9 行）。
6. hero 的 `~` —— **v84 已去掉**。
7. 退出动效白边（S23）—— 见上「白边现状」，**仍在核查**。
8. joi.com 参考 —— **判为记错域名**，待维护者给出正确链接。
