# 决策记录（Decision Log）

记录「评估过但当前**不落地**」的方向与理由。原则见 `CLAUDE.md`「v2 锁死 & 别镀金」：满 28 天真实记录、求职有实质进展前，不做可扩展分类法、人类报表、跨设备同步、登录、云端。**最大风险＝用打磨工具逃避面试推进。**

---

## D1 · 版本回退 / 快照（暂不做）

**评估日期**：2026-06-30 · **状态**：不落地

**现状真相**：
- 数据只有单一状态 `localStorage['timelog.v1']`，**没有历史/快照**。
- 导入是**按 id 合并**（`src/storage.js` `mergeImportedEntries`），**不是替换**。
- 因此**当前事实上没有「回退」能力**：导入旧备份只能并回旧条目，无法移除「旧备份里没有、但之后新增/删除过」的条目。把「导入旧备份」当回退用会得到错误结果。

**决策**：满 28 天真实记录前不做。回退/快照属于状态历史管理，紧邻被锁死的「可扩展/同步」边界，收益不抵复杂度与跑题风险。

**若将来真要做（不是现在）**：最小真回退原语＝「导入时提供『替换而非合并』选项」（一次性整体替换当前 `entries`），而**不是**引入多版本快照栈或云端历史。先有这个原语，配合现有「下载 JSON」就能手动回退，无需新数据模型。

---

## D2 · 付费转型 / 多人维护（暂不设计）

**评估日期**：2026-06-30 · **状态**：不落地

**疑问**：之后若转付费、且有其他开发者帮忙维护，该怎么处理？

**回答（付费与维护是两件独立的事）**：
- **付费**：时间尺是本地优先、零后端、无账号的静态 PWA，没有可计费的服务端。任何直接计费都会撞「无后端 / 无账号 / 无云同步」铁律。可设想的最小侵入路径是「赞助 / 一次性买断 + 可选云备份增值」，但这要等 v2 之后、且求职有实质进展再议。
- **多人维护**：靠**许可证 + `CONTRIBUTING` + 治理文档**约束，而不是改运行时结构。许可证已于 v31 落地（见下「许可证：已落地 AGPL-3.0」）；作为唯一著作权人保留商业双许可权，暂不需要 CLA。

**决策**：**许可证部分已落地**（AGPL-3.0，保护性、耐久、便宜）；**付费转型与云备份增值仍 gated**——现阶段（pre-28 天、求职未见实质推进）不设计、不落地，仅记录该决策本身。重申最大风险＝把「付费/治理规划」当成逃避面试推进的镀金。

### 许可证：已落地 AGPL-3.0（2026-07-01，v31）

**选择**：`AGPL-3.0-or-later`（非 MIT），配「作者保留商业双许可」姿态。`LICENSE` 全文 + 每个运行时文件 SPDX 头 + README「许可证与版权」节 + `CONTRIBUTING.md` 均已落地。

**理由**：
- **AGPL §13 堵住 SaaS 闭源克隆**：客户端 JS 本就完全可读，MIT 等宽松许可等于放任他人闭源套壳做成付费服务。AGPL 的网络条款要求「对外提供服务即须开源」，是对纯前端项目为数不多有牙齿的威慑。
- **唯一著作权人保留商业双许可**：现在只有一个作者，无外部贡献，可无 CLA 地在 AGPL 之外另行授权；这为将来「一次性买断 / 可选云备份增值」留了法律口子，又不必现在就设计付费。
- **法律威慑 + git 溯源即最优解**：前端代码无法技术性防拷贝，务实止损＝清晰许可（威慑）＋ 公开 git 历史（作者身份与时间线的可追溯证据）。

**未来可选项（备注，不做）**：软件著作权登记、商标注册——都属「求职有实质进展后再议」，此处仅登记备选，避免日后追溯。

---

## D3 · 推广策略（gated，暂不主动执行）

**评估日期**：2026-07-01 · **状态**：不落地（推迟主动推广）

**背景**：需求里含「写推广文案 / 选渠道」。主动社区推广正是 `CLAUDE.md`「别镀金」点名的 #1 风险——**用运营/推广动作逃避面试推进**的高发区。经评估重框为「求职证据工作」：本轮只做便宜、耐久、保护性的产出（许可证、博客草稿），**主动推广推迟**到博客产物就位或出现真实使用里程碑之后。

**决策**：现阶段不主动推广。先让「开发博客」作为可写进简历/面试的工程叙事产物落地；等有真实用户信号或求职实质推进后再谈渠道执行。

**渠道建议（就位后再用，不是现在）**：
- **中文社区先拿真实用户**：少数派 / 即刻 / V2EX / 掘金等，受众与「本地优先、隐私、零依赖」调性契合，且应用界面是中文，落地摩擦最小。
- **英文社区用「博客 dev-story」当内容源**：应用 UI 是中文，不适合直接向英文用户推应用；但**工程故事**（零依赖无构建 PWA、红线治理、postmortem 根因文化）可作为英文 dev-story 内容源投递 —— 正是「英文版当内容源、而非推应用」的设想。

**可核实的事实卖点（避免夸大，仅这两条已核对属实）**：
- **不主动保持屏幕常亮**：应用未使用 Wake Lock，不会强制屏幕常亮，符合「轻量、不打扰」定位。
- **完整备份是 Markdown 摘要 + JSON 导出**：摘要导出为 Markdown（可直接贴给 AI 复盘），完整备份为 JSON（复制/下载/分享），均在本地完成、不上传。

**重申**：以上属 gated 规划，登记以备日后，不构成本轮开工项。最大风险＝把「推广规划」当成逃避面试推进的镀金。

---

## D4 · 日视图交互调研（v36；提案 F 于 v45/v48 落地）

**评估日期**：2026-07-07 · **状态**：提案 E 仍为原型；提案 F 经用户授权于 v45 初版、v48 完整落地

**背景**：v34/v35「直接操纵 rail」（拖边界改时间）SE2 真机验收后被判定观感不及预期，v36 已回退为 v33 卡片列表（见 `CLAUDE.md` CHANGELOG v36、`docs/postmortems.md` P17）。用户要求研究是否有更优雅的日视图交互方向。为避免重蹈 P17——「demo 数据必须覆盖真实使用会撞到的极值分布，均匀数据会系统性藏起钳制类缺陷」——两个原型的 demo 数据统一采用真实分布：近 8h 整块睡眠、4~4.5h 专注工作块、外加一批 20~45min 碎片（洗漱/通勤/吃饭/刷手机），不用均匀数值。

**调研输入**：WebSearch 时间记录类 App 的日视图/列表交互模式（Toggl Track、Timery、ATracker 等）。确认 Toggl Track 官方模式为「Timer 屏幕的记录支持左滑删除/收藏」，是验证过的真实产品模式，而非臆造。

**曾产出两个可交互原型**（纯 demo 数据，非运行时资产；v53 已删除可执行副本，历史代码由 Git 保留）：

1. **`day-band.html`（提案 E · 比例日程带）**：日视图顶部加一条纯只读、不可拖的比例日程带——每段高度严格按分钟数比例分配，不做钳制、不做像素:分钟实时映射，因此结构上不可能重演 P17 的拖拽回弹问题。点一段只弹出摘要 popover（时间范围 + 内容 + 「编辑这条」跳转按钮），真正编辑仍下沉到下方卡片列表（与已发布运行时同款交互，零改动）。极薄碎片（视觉高度 <22px）自动隐藏文字、只留可点色带，避免挤爆。
2. **`day-swipe-cards.html`（提案 F · 滑动卡片）**：卡片右侧不再常驻编辑/删除图标按钮，改成左滑露出（Toggl Track 验证过的模式），正文因此更干净、留白更多；保留一个常驻「⋯」按钮兜底键盘/无触屏可达性——纯手势对键盘用户不可达，这是滑动模式必须补的口子。

**后续决策**：提案 E 仍未合入。提案 F 经用户明确授权后分两步落地：v45 先做左滑即编辑；v48 改为仅触摸/触控笔启用的 2×72px 编辑/删除轨道，补齐水平轴锁定、跟手、吸附、一次只开一张，以及纵向滚动/点空白关闭。原型里的常驻「⋯」兜底没有照搬，运行时用“点整卡编辑 + 编辑页删除 + 键盘 Enter/Space”提供完整非手势路径。

**剩余项**：提案 E 继续保持 gated，不因 F 已落地而自动开工。提案 F 的模型与双引擎自动化已完成，但 `touch-action`、惯性和纵横轴竞争仍必须在 Safari 浏览器与主屏 PWA 真机验收；手势类交互不能只靠 headless 宣布完成。

---

## D5 · 第三方产品审计意见的可行性分档（2026-07-08，gated）

**背景**：外部审计给出六个方向（原生 widget/灵动岛、半自动记录、界面冲击力、漏损预算、意义闭环、"三块空地"战略）。逐条对照本项目铁律（零运行时依赖静态 PWA、无后端/账号/云、数据只在 localStorage、v2 锁死）分档如下。**本条只登记判断，不构成开工项。**

### 档一 · 结构性做不了（web 平台无 API，需原生载体）

- **原生 widget / 灵动岛一键记录**：WidgetKit/ActivityKit 只对原生 App 开放，PWA 无任何入口。做它=转原生（Swift + App Store 分发）=产品形态转向，须另立决策，不是"加个功能"。
- **屏幕使用时间自动填充**：Apple 不向任何第三方（含原生 App）开放可导出的 Screen Time 原始数据（DeviceActivity 只给扩展内聚合报表），web 侧为零。**结构性不可行，与是否转原生无关。**
- **睡眠数据自动填充**：HealthKit 仅原生。web 不可行。

### 档二 · 可行但只有降级形态（28 天门后再议）

- **快捷指令近似"一键记录"**：iOS 快捷指令（可放主屏/锁屏 widget）打开 `?quick=switch` 之类带参 URL，`app.js` 启动时解析参数直接开表单。体验是"打开 PWA + 自动弹表单"，非真·一键，但零依赖、零后端。改动小，属工具打磨，门后做。
- **日历 .ics 本地导入（半自动消未记录）**：用户手动导出/分享 .ics，应用本地解析、按时间段提议成记录（复用 `mergeImportedEntries` 的确认式合并姿态）。无 OAuth、无 CalDAV、无云，隐私模型兼容。是"半自动记录"里唯一够得着的一块，但是真功能，门后做。

### 档三 · 纯本地可行、门后优先级最高（审计说的"结论力 + 意义闭环"恰好全在这档）

1. **异常天标记（T13）**：月/年汇总行按阈值（漏损%/未记录% 超标）标色/标记。纯 `stats.js` 层小改动，账本数据现成。**门后第一顺位**。
2. **漏损预算**：config 加一个"本周漏损 ≤N h"字段，ruler/摘要标注进度与超标。同样是统计层小改动。**门后第一轮可与异常天同批**。
3. **周回顾仪式 + 漏损账单**：`summarizeRange` 机制现成；加时薪 config 字段（localStorage，本地敏感数据可接受）即可算"本周刷手机 14h ≈ 时薪×14 机会成本"。**门后第二轮**。
4. **基于数据的下周建议**：内建 AI 违反零后端铁律，不做；但项目已有"摘要贴给 AI"闭环——便宜的增强是摘要导出里加周环比 delta，把闭环的原料喂得更好。规则式建议（如"漏损集中在 22:00 后"）可作为周回顾的一部分。

### 档四 · 观感/定位类（park）

- **人生格子图（LifeWidget 式）**：情绪冲击便宜是事实，但它不消费账本数据（纯出生日期算术），且审计自己也说"别跟 LifeWidget 比皮肤"。park，不排期。
- **ADHD 人群定位**：营销文案，归入 D3 推广 gate，随 D3 一起解冻。

### 对"三块空地"战略的修正

审计圈的三块空地里，**"记录自动化"这块对 web PWA 是半封死的**（widget/系统集成需原生；Screen Time 对谁都封死）——本项目真正够得着的是**结论力**（异常天、预算）和**意义闭环**（周回顾、账单、环比摘要），两者全是纯本地统计层改动，恰好也是四桶 taxonomy 这个核心资产的自然延伸。若某天判断"记录自动化"值得全力去占，那是「转原生 App」级别的产品决策，须另立条目评估，不在本仓库红线内。

**重申 v2 锁死**：以上全部 gated 在「28 天真实记录 + 求职实质进展」之后；本条审计意见本身就是最典型的镀金诱惑源——账本还没记满一个月，先别给账本装审计委员会。

---

## D6 · v55 功能冻结后视觉收敛豁免

**评估日期**：2026-07-11 · **状态**：明确批准并锁定范围

日视图已确诊为 card soup：tabs、日期导航、周期标签、hero 与每张记录卡共用 `--card + shadow-1 + top-light`，导致所有表面视觉重量相同，今日结论无法成为主角。参照 v33/v34 UI 重构的豁免先例，本轮在功能冻结后仅偿还这一项明确遗留视觉债；范围锁死为纯 CSS 的弱卡片化、强调色预算与密度微调，不做连续日志容器，不动 DOM、模板、业务 JS、左滑轨道、刷新快照或动画约束，也不由此解冻任何其它镀金事项。

---

## D7 · 里程碑、支持作者与主线高级设置（gated，待调研）

**评估日期**：2026-07-13 · **状态**：记录方向，不构成开工项

### 里程碑按累计有效记录日研究

未来若研究里程碑，采用“累计有效记录日”而非连续签到；中断不会把累计值清零。首阶段只考虑一次性、温和的庆祝反馈，徽章系统或独立进度页更后置；节点必须由真实使用证据决定，不照抄 `3/7/21/100`。

“有效记录日”目前**尚未定义**，后续调研必须明确回答：

- 是否只按不同本地自然日计数；
- 只有计划、没有已发生记录的日期是否计入；
- 导入的历史记录是否计入；
- 删除当天记录后累计值是否回退。

无论答案如何，这一方向继续受“累计 28 天真实记录 + 求职有实质进展”的总 gate 约束；本条不授权实现庆祝、徽章或进度页。

### 付费方向改为“支持作者”

若未来设置金钱入口，定位为“支持作者”，只放在“更多”里，且所有功能始终免费。支持不购买功能、服务、股权、优先权或产品路线影响力；款项由作者自由支配。月度支持与 pay-what-you-want 只作为备选形态，当前不选择、不实现。

本条明确 **supersede D2 中“一次性买断 + 可选云备份增值”的付费方向**。D2 已落地的事实仍保留：仓库使用准确的许可证标识 `AGPL-3.0-or-later`，公开源码，作者保留商业双许可的可能性。仓库继续公开；把仓库改私有不能保护已经部署、浏览器可读取的前端源码。官方渠道、项目名称和图标的防冒充规则只列为后续评估，不在本轮实施。

### 主线高级设置交由 Fable5 后续调研

主线高级设置本轮不实现，交由 Fable5 后续调研。四类动作必须分别评估，不得当作同一种“改配置”：

- **改名**：必须迁移历史标签或建立别名，否则旧记录会成为孤儿标签；
- **改桶**：会追溯性重算全部历史统计；
- **停用**：只应阻止新记录选用，不能破坏历史记录的归类；
- **`longOk`**：会追溯改变长段待确认口径。

**决策**：以上三组方向只登记边界和待研究问题，不进入 v57 运行时。最大风险仍是用产品化、商业化或设置扩张逃避面试推进。

---

## D8 · 发布存续、原生载体与 WebDAV 备份（gated，当前不实现）

**评估日期**：2026-07-14 · **状态**：记录边界与候选路径，不构成开工项

### 背景：用户信心其实包含三个不同问题

本条源于对“仓库转私有后 GitHub Pages 是否仍可使用”“PWA 缓存被系统回收后还能否启动”“是否需要原生 App 才能给用户长期信心”的连续讨论。三个问题不能混为一谈：

1. **源码可见性**：仓库是否公开，只控制 Git 历史、issue、文档和未发布文件的访问权限。已经发到网页端的 HTML/CSS/JS 必须交给浏览器执行，不能靠私有仓库隐藏；已经被 clone、下载或形成公开 fork 的副本也收不回来。
2. **运行时可获得性**：用户关闭页面后，是否还能取得启动 App 所需的静态文件。当前 PWA 靠 GitHub Pages 在线源站和 Service Worker 缓存共同保证；缓存是浏览器管理的 best-effort 存储，不是永久安装包。
3. **数据可恢复性**：设备丢失、卸载、清站点数据或浏览器回收存储后，记录能否恢复。原生安装包只改善运行文件的持久可获得性，**不会自动备份用户数据**；WebDAV/文件备份解决的是数据恢复，也不会让已经下线且缓存已失的 PWA 重新启动。

### 当前事实与承诺边界

- GitHub Free 个人/组织账号把发布中的仓库改为私有时，GitHub 会自动下线其 Pages；GitHub Pro/Team/Enterprise 可从私有仓库发布 Pages，但站点默认仍可向公网开放。私有仓库不等于私有网站。参见 [GitHub 仓库可见性说明](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/managing-repository-settings/setting-repository-visibility) 与 [GitHub Pages 建站说明](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)。
- 当前 `sw.js` 会预缓存全部运行时资产，并采用 cache-first 读取。已经访问或安装到主屏的设备在源站暂时不可用时通常仍能启动；但若 Cache Storage/Service Worker 被系统回收，且源站同时下线，刷新或重启后就不能重新取得 App。
- 页面仍开着时，已载入内存的代码可能暂时继续工作；这不构成关闭、刷新或重启后的可用性保证。
- 数据权威仍是当前 origin 下的 `localStorage['timelog.v1']`。仅运行缓存被回收时，记录可能仍在；若用户清除整个站点数据、卸载导致容器被清除、设备损坏或系统一并回收 origin 存储，记录也会消失。
- 当前可诚实承诺的是“本地优先、完整导出、稳定公开地址下可离线使用”，不能承诺“安装一次后，即使作者永久停服也一定能启动”或“本机数据永不丢失”。

### 候选路径 A：继续稳定托管 PWA（当前基线）

保持固定公开 URL 和静态源站，是成本最低且符合本项目身份的方案。用户清掉运行缓存后，只要源站仍在，浏览器就能重新下载 App；已有 `localStorage` 未被清除时，记录会重新出现。

这条路径的可信度来自：长期稳定的访问地址、清晰的本地数据说明、完整备份/导入能力、可验证的版本发布和停服前充分通知。它不要求原生 App，但也不提供脱离源站的永久启动保证。

仓库当前继续公开；若将来只想隐藏 Git 历史而保持公网 App，应先确认付费套餐和 Pages 设置，并认识到浏览器拿到的前端源码仍然可读。不要把“仓库转私有”当作代码保护方案。

### 候选路径 B：可独立启动的安装包（未来须另立项目）

若未来要作出更强承诺——“用户取得某个版本后，即使源站关闭，仍能从设备上的完整运行文件启动”——就需要把运行时资产随安装包交付。可以是：

- 用 Swift/Kotlin 重写的原生 App；
- 内嵌现有 HTML/CSS/JS 的原生壳；
- 桌面端打包载体或其它能把静态资源真正随安装包分发的方案。

不必为了离线启动保证重写全部 UI；关键是运行文件是否属于安装产物，而不是界面是否使用原生控件。但无论采用哪一种，签名、构建、商店/侧载、升级、平台审核和原生存储都会引入新的工程边界，直接违反本仓库“无构建 / 无运行时依赖 / 单页静态 PWA”的身份。因此必须放在**独立仓库/独立产品载体**中评估，不能把包装工具链塞进本仓库。

原生载体仍不能替代备份：卸载、清除 App 数据、设备丢失或换机仍会造成记录不可用。原生的额外价值是可进一步接入系统文件、后台任务、WidgetKit/ActivityKit 等平台能力；是否值得承担长期维护成本，要由真实用户需求证明。

### 候选路径 C：WebDAV 作为用户自有的备份通道（不是同步）

WebDAV 值得保留为门后候选，因为它可以让用户把备份写到自己选择的服务（例如支持 WebDAV 的网盘、NAS 或自建存储），不要求时间尺运营账号或自建业务后端。但若评估，第一版只能定义为**备份传输层**：

- 上传时间戳命名、不可变的完整 JSON 快照，避免静默覆盖唯一副本；
- 明确显示最近一次成功时间、文件名、大小与失败状态，网络失败不能伪装成已备份；
- 恢复必须由用户显式选择备份并预览，复用现有安全导入；若需要真正回到某一时点，应先实现 D1 所述“整体替换”原语，不能把按 ID 合并冒充恢复；
- 不做后台双向同步、不做最后写入者静默覆盖、不做多设备实时合并，也不引入时间尺账号；
- 默认保持端到端的本地隐私边界。若远端文件需要加密，应在客户端上传前使用 Web Crypto 加密，并先设计恢复密钥、忘记密钥和跨设备解密流程；“加密了但用户无法恢复”不算可靠备份。

纯浏览器 PWA 落地 WebDAV 有现实限制，评估时必须逐项验证：

- 远端必须使用 HTTPS，并正确允许浏览器 CORS；很多 WebDAV 服务默认不允许任意网页 origin 直接请求；
- 用户名、密码或 token 如何保存是安全问题，不能把长期明文凭据直接塞进 `localStorage`；
- iOS/PWA 后台执行不可靠，不能承诺关掉 App 后定时自动上传；
- endpoint、认证方式、`PROPFIND`/`PUT` 支持、配额、证书和代理兼容性差异很大，需要真实服务矩阵测试；
- 若用原生网络桥绕开 CORS/后台限制，就已进入候选路径 B 的独立原生载体，不再是本仓库的零依赖纯 Web 实现。

因此 WebDAV 的合理首版姿态应是“用户主动连接并手动备份/恢复”，不是“无感云同步”。在此之前，现有“存储备份”写入系统文件选择器（用户可自行选 iCloud Drive/其它文件提供方）仍是更简单、可理解、无需保存远端密码的基线。

### 解冻前必须回答的问题

即使跨过“28 天真实记录 + 求职有实质进展”的总 gate，也不能直接开工。须先完成一份小型设计评审，至少回答：

1. 要提升的具体承诺是哪一个：源站下线后可启动、换机可恢复、自动备份，还是多设备同步？
2. 有多少真实用户遇到过缓存回收或数据丢失，现有手动备份为什么不足？
3. 备份是追加快照还是覆盖单文件；保留多少版本；如何校验完整性；如何做一次真实恢复演练？
4. WebDAV 凭据、远端文件和本机数据各自的威胁模型是什么？忘记密码/密钥时如何诚实失败？
5. 浏览器/iOS 主屏 PWA/未来原生载体分别能保证什么，哪些能力只能 best-effort？
6. 独立安装包的发布、签名、更新和停止维护成本，是否真的小于“稳定托管 PWA + 文件/WebDAV 备份”的组合？

### 当前决策

- **现在不转原生、不接 WebDAV、不做同步，也不改变现有运行时。**本条仅把讨论和边界落盘。
- 当前继续以稳定公开 PWA + 完整手动备份作为基线；用户信心文案必须使用可兑现的承诺，不把 Service Worker 缓存说成永久安装。
- WebDAV 保留为未来“用户自有远端的手动/可验证备份”候选，优先级低于把现有导出—恢复流程验证扎实；它不得绕过 v2 对云端/跨设备同步的锁定。
- 若未来决定保证脱离源站仍可启动，另立原生/打包载体项目；若未来决定保证数据可恢复，单独设计备份与恢复。两项可以组合，但不能互相冒充。

重申最大风险：这类“长期产品化基础设施”很容易成为新的镀金方向。没有真实使用证据和求职实质进展时，正确动作是记录，而不是开工。

---

## D9 · 外部验证延期，进入 14 天自用冻结（2026-07-15）

冻结周期：2026-07-16 00:00 → 2026-07-29 23:59（GMT+8）；复盘 2026-07-30。
完整执行边界见 `docs/dogfood-freeze-handoff.md`。

### 背景与事实

`docs/product-direction-validation-handoff.md` 规划的外部招募、12 次访谈和 8 人 7 天试用**全部延期**。

必须准确陈述原因：

- **延期不是「验证通过」，也不是「验证失败」**——外部验证**尚未开始**，没有产生任何外部证据；
- 真实原因是维护者**当前没有候选用户**，并明确选择先把产品作为个人工具跑通闭环；
- 任何后续文档、作品集或对外表述都**不得声称市场需求已被验证**。

### 决策

- **当前 V1 核心 = 以尽可能低的管理成本留下可信行动记录，让自己第二天仍能知道昨天做过什么。** 这是当前唯一有个人证据支持的价值层。
- **「识别目标偏差」「AI 复盘导致第二天改变行为」降为候选增值假设**，不再是 V1 验收条件。「内建 AI / Agent 集成」冻结不实施。
- **14 天内功能冻结**：只允许处理数据损坏、无法启动、无法记录/编辑/导出/恢复、离线不可用等阻断问题，且仍需复现证据 + 最小改动 + 回退方法 + 自动化测试 + 维护者明确批准。
- **求职外部结果是本阶段的硬约束**：10 次高质量岗位投递 + 发布 1 个作品集案例。这条存在的意义是**防止工具打磨替代现实行动**——它直接对应本文件开头的「最大风险＝用打磨工具逃避面试推进」。
- **作品集不是新增功能的理由。** 案例只能基于已完成的工作和真实工程取舍整理。

### 与 28 天 gate 的关系

`CLAUDE.md` 的「28 天真实记录 + 求职实质进展」gate 保留，本冻结**更严格**：14 天内即使累计记录达到 28 天，也只表示**可以重新评审**，不自动解锁 roadmap。

### 退出条件

2026-07-30 复盘只允许三类结论：继续冻结并使用；只修排名第一的真实摩擦；停止主动开发、把时间转回求职和作品集。**禁止因为攒了一批想法就整体解冻路线图。** 只有真实使用反复暴露的损失，才有资格成为下一轮开发输入。

---

## D10 · AI 协作窗口关闭，候选定向提前处置（2026-07-18）

### 背景与事实

- 维护者 2026-07-18 告知：对当前协作模型的访问窗口即将结束。D9 制定时未考虑「AI 执行杠杆是限时资源」这一变量。
- 维护者明确分工：求职硬约束改由其它模型支持（硬约束本身与优先级**不变**）；本模型剩余窗口专注本项目。
- 触发方式：维护者在逐项批准式询问中明确选择「整体解冻分析，推进」。

### 决策（定向提前处置，不是整体解冻）

**提前执行**（一个开发期工具批次 + 一次 v67 发版）：

- **C11**（`bump_version` 脚本 + 模型层 ts-check）：纯开发期、零运行时字节。「没有 AI 后最容易犯错的两件事」优先工具化——这是本决定里唯一因窗口关闭而**价值上升**的类别。
- **C8 + 发版顺手项**（两处说明文案 + `recordedDayKeys` 去 export）：行为零变化。
- **C7 方案 A**（过夜写入即确认）：本批唯一的行为改动；已有复现、改动极小、有回归测试；修的是「用户以为记了、统计静默少算」的真实缺陷。同日早前「等 07-30 与 C5 并案」的决定被本决定修订；方案 A 与未来任何阈值策略兼容（只把「显式双端断言」标为已确认，不动阈值语义）。
- v67 发版本身顺带为 C1/C6（更新通道）再积累一个真机观察点。

**继续冻结**（不因窗口关闭而动）：

- **C1/C2/C6 的语义改动**：需要 v64 之后的真机数据先说话；近期可做的只有「诊断 v2」（补 SW 注册状态字段），列为后续批次，视窗口余量决定。
- **C3/C4/C5/C9/C10**：产品方向判断，依赖真实使用数据或独立决策；AI 窗口关闭不改变其评估条件——这些项的难点是设计，不是代码。

### 边界（防止本决定成为坏先例）

- D9 的「禁止因为攒了一批想法就整体解冻」**仍然成立**：本次判据不是「想法攒够了」，而是「执行杠杆到期，且所选项几乎全部零行为」。
- 28 天 gate、别镀金条款、外部验证「未开始≠通过」的表述边界全部不变。
- **2026-07-30 复盘照常举行**：评审 14 天记录数据与剩余候选；本决定不预支复盘对「记录可持续性」的结论。

### 模型路由（窗口关闭后的维护指引）

- 可交常规模型（如 Sonnet 5）：发版仪式（有 bump 脚本护栏后）、文案、测试维护、文档整理。
- 建议留给强模型：planner/确认语义等模型层改动、SW 更新链路、WebKit 布局怪癖类问题。

### 执行记（同日追补）

批次一（v67：C7A/C8/死 export/ts-check + C11a 脚本）与批次二（v68：诊断 v2——SW 注册态早读 + FCP）均已于 2026-07-18 执行完毕。C1/C2/C6 语义改动继续等真机数据；同日已录得首个干净自动更新正例（见 freeze-candidates C1）。

---

## D11 · 维护者批准的第三次冻结定向开口：底色冷移一档 + 阶段格言 v1（2026-07-20）

### 背景与事实

- 维护者 2026-07-19 深夜提出两件事：① 评估亮色主题要不要回退到 v33 之前的冷白色；② 新功能想法「阶段格言展示区」（展示当前阶段对自己有用的一句格言，未设置用默认）。
- AI 按惯例给出评估 + 冻结提醒 + **「最大风险＝用打磨工具逃避面试推进」的明确风险提示**（CLAUDE.md 要求在此类时刻必须指出），并把决定权交回维护者。
- 维护者在一次性批量决策（随后离线休息，授权 AI 通宵执行完整发版）中明确选择：
  - 冷白问题：**不整体回退，折中——只把亮色底色冷移一档**；
  - 格言功能：**实现并完整发版**（合并 main + tag + GitHub Release），不等 07-30 复盘。

### 决策

1. **亮色底色**：`--bg` 从宣纸暖 `#f7f5f1` 改为近中性 `#f6f6f5`（两处主题块 + `theme-color` 两锚点 `index.html`/`app.js`；manifest `theme_color` 是暗色值，不动）。**只动底色**，`--card`/`--input`/`--track`/`--border`/阴影暖调全部保留。对比度重校（WCAG）：全部文字令牌对新底比值持平或微升（正文 11.70:1、muted 5.27:1、最低的 danger 3.98:1），卡底分离 1.089→1.081，仍由阴影+发丝线承担。
2. **阶段格言展示区 v1**：完整规格与验收标准见 `docs/freeze-candidates.md` C13；随 v69 实现。
3. 两项合并为一次 v69 发版，走完整版本仪式与全量自测。

### 诚实代价（不粉饰）

- **这是冻结期第三次开口**（D10 批次一/二之后），且判据不再是「零行为改动」——格言是新的用户可见表面。它不符合 D9「只修阻断」的字面边界，依据的是维护者明确批准这一唯一例外通道。
- **风险提示已给出、维护者知情选择**：本次开口发生在求职硬约束（10 投递 + 1 作品集）尚未见记录进展的情况下；07-30 复盘评审「记录可持续性」时，本次开口应作为「工具打磨引力」的一个数据点如实计入。
- **底色冷移一档部分走回 v33「宣纸暖」的设计立场**：v33 的亮色身份即暖底，这次只动 `--bg` 留下「中性底 + 暖调 input/track/边框」的轻微不对称——被有意接受（这些暖令牌几乎全部落在白卡面上，不与底色直接相邻）。若一档不够、还想要整套冷色板，那是 07-30 之后的另一次决策（完整回退需重校三桶彩色双主题，代价见 C12）。
- **通宵执行意味着真机验收发生在合并与发布之后**：v69 的双引擎测试与截图自验由 AI 完成，维护者真机核对是事后把关；若真机不符，回退路径＝revert PR + v70 发版（SW cache-first 决定了任何修正都要走完整版本仪式）。

### 边界

- D9 的 07-30 复盘照常举行，本决定不预支任何复盘结论；其余候选（C1–C6、C9、C10）继续冻结。
- 格言 v1 不做的事也在此锁定：不做多条格言轮换、不做按日期/阶段自动切换、不做格言历史——这些都是「可扩展分类法」的近亲，撞「别镀金」条款。

### 执行记（同日追补）

同一批次追加第三项：**C14 第三桶改名「漏损」→「偏航」**（维护者原话「适时地放空是必要的」）。判据与前两项一致——维护者明确要求、改动面是显示名与文案、内部键 `leak` 不动因此零数据迁移。它比前两项更贴近 D9 的 V1 核心：道德化命名会抑制如实记录休息，改名直接服务「以尽可能低的管理成本留下可信记录」。**但它同样是冻结开口，同样计入 07-30 复盘的「工具打磨引力」数据点。** 明确没做的事见 `docs/freeze-candidates.md` C14「诚实边界」——改名不重新归桶，「发呆算不算必要消耗」仍是复盘的开放问题。

**启动诊断的处置（同日判读）**：22 条样本已回答它被打开时的问题——SW 全程接管、常驻存储 22/22 为真、缓存恒为完整 21 文件、就绪 355–735ms，且长间隔样本（14h42m→355ms、12h34m→518ms、8h45m→635ms）**不比短间隔慢**。因此 P33/C6 的「iOS 长间隔回收 Cache Storage 导致起床首开特别慢」假说**在页面侧被证伪**：慢段确实落在页面计时零点之前（与 v66 录屏判决一致）。唯一尚未取证的是 v68 新增的 `SW态` 字段——本窗口 9 条全为 `a:activated`，但期间没有发生任何版本更新，该字段只在更新交接那一刻才有信息量。**处置：留到 v69 更新落到真机、取得那一两条跨版本样本后即可关闭**（更多 → 启动诊断 → 关，样本整键删除）；不必开到 07-30。

**2026-07-20 追补（v69 交接样本已取得，诊断可关闭）**：25 条样本收齐，两件事定案。① 冷 navigate 样本（n=18，排除更新后紧邻的热启动）**间隔与就绪时间相关系数 +0.05**，长间隔（≥4h）均值 516ms、短间隔（<1h）466ms，差值远小于组内波动（短间隔自身横跨 155–735ms），且全场最快的冷启动恰是间隔最长那次（14h42m→355ms）——「起床/久不开首开特别慢」在页面侧**再无立足点**，慢段确实在页面计时零点之前，本仪器原理上看不见它，**已到测量上限**。② v68→v69 交接首次取到 `SW态` 读数：`a:activated`，无 waiting 悬挂（C6 反证），但维护者确认期间**完全退出过**，故自动链路独立切版仍零正例。**v69→v70 交接后样本升至 4/4**：该次由前台恢复（`visibilitychange → reg.update()`）而非冷启动触发，形态却完全一致，维护者同样确认完全退出过——说明问题不限于某条触发路径，而在 reload 动作本身拿不到新版本（详见 `docs/freeze-candidates.md` C1 盘点表与收窄后的问题定性）。**结论：启动诊断的三个原始问题全部有答案，即可关闭**，不必开到 07-30。

---

## D12 · 域名迁移 `time.eigentime.org`（冻结期第四次定向开口：只做零运行时准备，发版序列待 07-30 复盘后）（2026-07-24）

### 背景与事实

- 维护者 2026-07-24 带两个提案进入 grilling 评审：①「收敛/预计结束—实际结束—延期原因」结构化字段；② 迁移到个人域名 `time.eigentime.org`（双仓库拓扑，ADR 已先行写在 personal-blog 仓库 `docs/adr/0001`）。
- AI 按惯例给出冻结提醒与**「最大风险＝用打磨工具逃避面试推进」的明确风险提示**（并指出维护者自己委托的 ChatGPT 证据包在「暂缓」一节明确列有「仅为自定义域名或视觉效果进行开发」），把决定权交回维护者。
- 维护者知情选择：**申请第四次冻结开口（继 D10/D11），范围只含域名迁移**；结构化字段功能**不开工**，归档为 `docs/freeze-candidates.md` C16。

### 决策链（grilling 逐项裁决）

1. **拓扑**：`wowayou/time-logger` 是唯一源码仓库与发布入口；另建公开部署镜像 `wowayou/time-logger-site`（只含生成产物，禁止手工维护业务代码），绑定 `time.eigentime.org`——`/` 为产品主页，`/app/` 为固定 PWA 入口。旧 origin `wowayou.github.io/time-logger/` 按 ADR 三步处置：迁移横幅版 → 绑域名 → 只读冻结版。完整契约见 personal-blog 仓库 ADR 0001；本条是其在本仓库的同步与裁决记录，两处冲突时以本条与本仓库红线为准。
2. **日历切分**：冻结期内（→07-29）**只做零运行时准备**（本仓库文档修订、`scripts/build_site.py`、`site/` 主页源码、publish workflow、部署仓库与 DNS 预置）；**三步发版序列（v71 横幅、绑域名上线、v72 只读冻结）07-30 复盘后启动**——复盘若结论为「停止主动开发」，发版序列作废，已建的部署仓库与 DNS 记录无害闲置。
3. **只读冻结页形态**：完整应用 + 只读开关（隐藏 FAB、禁用新建/编辑/导入等写路径，保留导出/分享/时间轴浏览、常驻迁移横幅），**不做**独立精简导出页——后者是第二套 artifact，违背「双仓库不是两套代码」的自我约束。
4. **发布机制**：`scripts/build_site.py`（标准库、确定性组装、运行时清单**直接解析 `sw.js` 的 FILES 数组**为单一真源）+ 源仓库 Actions 薄层（vN tag push 触发，fine-grained PAT 仅授权部署仓库 contents:write，一年期）；PAT 失效＝workflow 红灯可见 + 本地同脚本手动兜底。选自动化而非纯手动的理由是 v59 学费：人肉多步仪式必漏，且「忘跑发布→新域名静默旧版」比红灯难发现。
5. **备份格式**：**零改动**（执行期修正——grilling 时计划「v71 加 `meta.schemaVersion`」，落地核对发现 `exportData()` 已写入顶层 `version: 1`，备份本就是版本化 JSON，不再重复加字段）；ADR 的「校验值」要求**删除**——现有整批逐字段预检才是真完整性门闩，校验值会拒掉 dogfood 协议里合法的 GMT+8 分析副本，或逼出让它形同虚设的宽容路径。
6. **技术事实（已验证）**：manifest `scope:"./"`、SW 注册与 FILES 全相对路径 → 应用搬到 `/app/` 子路径零运行时代码改动；现有导入已满足 ADR「稳定 ID + 幂等合并 + 冲突预览」要求，迁移的导入侧零新开发。
7. **权威设备迁移**（07-30 后）：完整备份 → 新 origin 添加主屏 PWA → 导入（C6 已验证流程，`firstUsedDate` 随备份）；不依赖跨 origin 静默搬运。

### 诚实代价（不粉饰）

- **这是冻结期第四次开口**，且方向正是证据包「暂缓」清单点名的自定义域名。判据仍是维护者明确批准这一唯一例外通道；07-30 复盘评审「工具打磨引力」时本条如实计入。
- **未来两次旧站发版（v71/v72）都要过 C1 那条更新通道**——四次交接零正例、「完全退出」是唯一被证实可靠的路径；迁移横幅能否送达权威设备本身就是又一次 C1 观察点。
- **求职硬约束（10 投递 + 1 作品集）仍未见记录进展**，本次开口不改变其优先级；域名与主页对作品集有辅助价值，但「作品集不是新增功能的理由」条款不因此松动。
- 发版序列后置的直接动机之一：权威设备正在跑 14 天 dogfood 观察窗，窗口内换 origin/重装 PWA/导出导入会污染「记录可持续性」的复盘证据。

### 附带协议修订（同次 grilling 裁决）

`docs/dogfood-freeze-handoff.md` §3「不把原始或派生 JSON 提交仓库、发给 AI 或上传第三方」中「发给 AI」一项与实际实践（ChatGPT 证据包、AI 复盘闭环）已事实性分裂，修订为：不进仓库/公开渠道的硬边界不变；发给 AI 服务属维护者知情选择，须当次有意识决定，且记录含他人可识别信息（公司、人名）时发送前须意识到这不只是本人隐私。死规则腐蚀整份协议的权威性，认可现实并划清边界优于留着一条已被常规违反的禁令。

---

## D13 · 提前终止 D9 冻结，转入发版上线冲刺；确立「规格—执行—验收」多模型协作（2026-07-24）

### 背景与事实

- 维护者 2026-07-24（D12 落盘同日晚些时候）明确决定：**彻底抛开 07-30 复盘限制，集中力量快速完成基础发版和上线推广**。
- 这是维护者的知情选择：本会话内 AI 已多次给出「最大风险＝用打磨工具逃避面试推进」的风险提示（见 D12），维护者仍明确选择提前终止冻结。按惯例如实记录，不粉饰。
- 求职硬约束（10 投递 + 1 作品集）**本身不因本决定取消**，但其「冻结期内并行完成」的时间绑定随冻结终止而解除；28 天 gate 与「别镀金」条款继续约束**功能扩张类** roadmap（发版上线不是功能扩张）。

### 决策

1. **D9 的 14 天冻结提前终止**（原定 2026-07-29 止、07-30 复盘）。D12 的「三步发版序列待 07-30 后」时序约束同步解除，序列本身（v71 横幅 → 绑域名 → v72 只读冻结）与全部质量门（版本仪式、全量自测、真机验收前置）**不变**。
2. **冲刺范围**：完成 `time.eigentime.org` 迁移三步、更新定位文案、准备并执行首轮中文社区推广（D3 的推广 gate 随之解除；D3 的渠道建议与「可核实事实卖点」约束继续有效）。
3. **多模型协作模式**（D10「模型路由」升级）：Fable 定方向/规格/验收，另一台机器上的 Sonnet 5（或 Opus 4.8 / GPT）执行，维护者做只有人能做的步骤（凭据、DNS、真机、发帖账号）。通讯通过本 GitHub 仓库：规格在 `docs/specs/`，执行走分支 + PR，验收＝PR 评审合并。协议全文见 `docs/collab-protocol.md`。
4. **产品硬约束固化**（2026-07-24 分析定案，随本决定入 CLAUDE.md 产品红线）：① 首次打开即用、无强制 onboarding；② 离线照常记录；③ AI/自动化不得悄悄修改原始时间线；④ 导出不依赖付费或云端账号。第五条「约 5 秒完成一次记录」为**未达标军令状**，是上线后第一轮产品迭代的裁决标尺（所有记录侧候选按「是否让中位记录耗时逼近 5 秒」取舍）。
5. **核心定位（工作假设）**：「5 秒记下真实做了什么——本地、离线、可信的一天时间线」。人机并行记录与关闭循环审计为差异化候选层，等外部真实用户信号再晋级。对外表述继续禁止声称市场需求已验证。

### 边界

- 隐私红线、零依赖铁律、版本仪式、提交推送前自测清单全部不变，对所有执行模型同等生效。
- 推广材料不得使用真实数据/截图（demo PNG 白名单机制不变），不得夸大（沿用 D3「可核实事实卖点」纪律）。
- 维护者手动步骤的唯一权威清单是 `docs/launch-runbook.md`。

### D13 追补（2026-07-24 同日）

- **亮色整体回归冷白**：维护者真机观感后决定「整体亮色风格回归原本的冷白色」，推翻 D11 的一档折中、执行 C12 评估过的整体回退选项（含 C12 列明的对比度重校代价）。规格见 `docs/specs/SPEC-004-v72-cold-light-theme.md`，占用 v72；SPEC-002（旧站只读冻结）顺延为 v73。
- **宣传设计参考**：维护者指定 https://otty.sh/ 为 landing 结构语言参考，落为 `docs/specs/SPEC-005-landing-v2.md`（短句诗式特性标题 + 真实画面 + 重复 CTA；像素不抄袭、诚实纪律不放松）。
- **执行记**：SPEC-001（v71 迁移横幅）与 SPEC-003（上线文案包）由 Sonnet 5 执行、Fable 验收合并（PR #25/#26），v71 已 tag + Release，publish-site workflow 首跑 10s 绿灯；`time.eigentime.org` 证书就绪、已强制 HTTPS，新旧两 origin 均运行 v71——多模型协作协议首个完整闭环成立。

---

## D14 · ScreenCam 式盈利路径调研（登记不落地，2026-07-24）

### 背景

维护者提供参考案例：ScreenCam（macOS 录屏工具）「公测结束 → App Store 上架 → 订阅制 + 1 年兑换码赠送 → 官网开源」，问该路径对时间尺的适用性。

### 调研事实（2026-07-24 核实）

- **「开源」是宣传话术**：`motion-space/thescreen.cam` 仓库只是 landing page（MIT、10 stars、描述自称 "Landing page of ScreenCam"）；**App 本体闭源**。其路径实质是「闭源原生 App + 开源官网 + 公测养口碑 + 上架收订阅」。
- 公测通道停更但可继续用——即「旧通道冻结、新通道收费」，与本项目「旧 origin 冻结只读、新 origin 继续」在结构上同形，但**目的相反**（它是付费墙的护城河，本项目是域名迁移的善后）。

### 对照本项目的四个硬约束

1. **载体**：上架 App Store 需要原生载体——这是 D8 候选路径 B，明确必须**另立独立项目**，不进本仓库。触发门槛（D8/ADR 0001 一致）：Widget、锁屏快捷记录、Live Activity 等 Web 做不到的能力；仅为上架收费不构成触发。
2. **许可**：时间尺是**真 AGPL 全源开放**，与 ScreenCam 的「只开源官网」根本不同。AGPL 与 App Store 分发条款存在著名摩擦；可行出口是 v31 已保留的**唯一著作权人商业双许可**——自己给自己发商业授权上架。该出口在接受第一个外部代码贡献前有效，之后需 CLA 或重谈。
3. **付费形态**：订阅制与 D7 现行决策（「支持作者」：所有功能始终免费、支持不购买任何东西）**直接冲突**。若未来走「免费 Web + 付费原生壳（Widget 等原生增值）」，Web 端承诺可以不破——但那是显式推翻/修订 D7 的新决策，不能悄悄滑过去。
4. **时机**：ScreenCam 收费前有完整公测期养出真实用户与反馈。本项目当前外部用户≈0、外部验证未开始。**任何盈利设计在拿到真实留存信号前都为时过早。**

### 决策

登记不落地。可复用的顺序骨架：**公开自用/分享期（现在）→ 真实外部用户与留存（先决条件）→ 原生壳独立项目（D8-B，若原生能力有真实需求）→ 届时再决定收费形态（修订 D7）**。触发重估的条件：出现稳定外部用户群，或对 Widget/锁屏记录类原生能力的真实反复请求。届时另立决策，本条只存档调研结论。

---

## D15 · 额度约束下的队列裁剪与固定成本治理（2026-07-25）

### 触发

维护者告知 AI 额度余量不多，要求对当前执行队列做裁剪并留档。裁剪前队列：SPEC-012 → 009 → 011 → 010 阶段一 → 008 → 007，五单四次发版仪式。

### 成本结构认定（裁剪依据）

单个规格的成本大头不是写代码，是每单的**固定税**：版本仪式六锚点 + CHANGELOG 行；264×2 双引擎回归全量复跑（执行方一次、Fable 复核一次）；tag / Release / 镜像发布；以及三个模型各自把 `CLAUDE.md` 整读进上下文。

结论：**合并发版是最省的杠杆**——同样的代码量，四次仪式变一次，固定税砍掉约 3/4。

### 事实核查一则（改变了 SPEC-009 的优先级）

SPEC-009 的卡死实录是「v71 · 缓存 timelog-v72」。v71 是迁移横幅版本、只在旧 origin 有意义；新 origin 的 PWA 是 runbook Phase C 期间新装，起步版本只可能是 v72/v73。维护者确认：**卡死的是旧 origin 图标**——C2 第 6 步保留作对照、Phase D 收尾即删除的那个；新 origin 注册从未卡死。

### 决策

1. **v74 合并批次**（一次仪式、一行 CHANGELOG）：SPEC-012（导入 toast 回归）+ SPEC-011（状态栏遮条）+ SPEC-009-lite。
2. **SPEC-009 收敛为 lite**：只保留「···」里常驻的「修复更新通道」cell（在线前置 + `sw.js` 探活 → `unregister` → `reload`），2 条测试；砍掉检测三条件、启动计数器、横幅第三态。自动检测方案**登记为「出现外部用户后重启」**，不销毁——外部用户≈0 时，检测的价值追不上其成本与误报风险。
3. **SPEC-010 阶段一收敛为单变体**：只出变体 A，B/C 不做；排在 v74 之后，维护者提前拍板则并入 v74 同一次仪式。
4. **SPEC-007 / SPEC-008 park 到 2026-07-30 阶段复盘**：007 是队列里最大、且唯一触及数据迁移路径（主线改名）的一单，触发它的原话只是「视觉可以优化」；008 是给外部验证尚未开始的产品做落地页配图。park ≠ 取消，规格正文与已作出的裁决全部保留。
5. **SPEC-002 不变**（blocked）：解锁后并入当时最近一次仪式，不为它单独发一版。
6. **CLAUDE.md 瘦身**：CHANGELOG v1–v65 历史行移到 `docs/CHANGELOG.md`，本文件保留最近八版与**全部**红线条文；正文 40.9K → 21.7K 字（文件 80.4KB → 44.6KB）。`project_audit.py` 的 15 条 CLAUDE.md 短语断言全绿——其中「tooltip hover 延迟 800ms」此前**只存在于 v12 历史行**（红线原句写的是「hover 延迟 800ms」），已在 UI 红线原句内补足字面，这也说明历史行一直在替红线扛断言，外移时必须逐条验证而非整段搬走。
7. **规格状态清账**：SPEC-001/003/004/005/006 早已合并却仍挂 `in-progress`，统一改 `done`（附 PR 号），避免执行方按 status 认领时重复认领。

### 边界（本决策不做什么）

- 不改任何红线条文，不降低验收标准：P35 红灯证明、双引擎全绿、隐私红线、版本仪式六锚点照旧。
- 不改协作协议本身；执行方仍按 `docs/specs/` 的 `status:` 认领，本决策通过改 status 生效。
- park 的三单在 07-30 复盘按当时额度与求职进展重排，不在本条里预判结论。

### 一条诚实记录

被 park 或收敛的三单（007 设置重设计、008 landing mock、010 第三轮亮色）都是打磨类。本次裁剪由额度触发，但与「别镀金」条款「最大风险＝用打磨工具逃避面试推进」同向。28 天 gate 与 07-30 复盘是下一个决策点，本条不预支那个决策。

---

## D16 · 提前阶段复盘（2026-07-26，维护者要求不等 07-30）

### 事实盘点（可核实的）

- **28 天 gate：已达成**。权威设备 header 显示「已记录 29 天」——即 `stats.js` `recordingMilestones` 口径下有真实记录的自然日数。按 CLAUDE.md，达成只意味着**可以重新评审**，不自动解锁 roadmap。
- **冻结状态**：D9/D10 的 14 天冻结已由 D13 提前终止，当前无冻结；D15 的额度裁剪是现行排期约束。
- **发版**：v71–v75 五个版本已上线；`time.eigentime.org` 与镜像均已生效（runbook A1/A2/B 已按可验证事实勾选并注明证据）。
- **外部验证：仍未开始**。Phase E 首轮推广未发生，`docs/promo/` 三份仍是 AI 底稿，外部用户 ≈ 0。
- **队列**：SPEC-001–012 中，除 002（已于本日解锁）外全部 done；007/008 由 D15 park。AI 侧无待办。
- **额度**：紧张，已是一等约束（执行方曾撞周上限中断）。

### 结论

1. **gate 达成不改变优先级**。28 天 gate 证明的是「这个工具对作者本人成立」——它成立了。但下一道门不是功能，而是**是否对作者以外的人成立**，而那道门至今没有人敲过。
2. **瓶颈是人肉步骤，不是 AI 产能**。AI 侧两天内交付两个版本、队列已空；唯一能改变项目处境的动作（Phase E 首轮推广）是维护者动作，已挂起数日。再多 AI 产出不会移动这个瓶颈——这一条是本次复盘最重要的判断。
3. **SPEC-007 / SPEC-008 维持 park**，理由比 D15 时更强：007 服务「已有用户的体验」，而用户数为 1；008（landing 活体 mock）服务「落地页的说服力」，而落地页访客未知。008 在推广发生、且确认落地页是转化瓶颈之后才值得做。
4. **放行 v76 合并批次**（一次仪式，全部是已发生事实的收尾，不是新功能）：
   - SPEC-002 旧站只读冻结（浸泡已确认足够）；
   - 控件表面分层修正（v75 真机反馈：亮色下 chrome 比 content 更重，层级倒挂；拆出 `--chrome` 令牌，壳比页面略亮、井保持凹陷）；
   - SPEC-011 遮条底缘 8–12px 渐变（真机验收发现硬切边，规格已预留此退路）。
5. **求职硬约束仍在**。CLAUDE.md 写明本阶段并行硬约束是 10 次高质量投递 + 1 个作品集案例，且「作品集不是新增功能的理由」。本次复盘**无法核实其进展**（AI 不可见），需维护者自行对账；若落后，AI 侧任何扩张应继续冻结。

### 下一个复盘的触发条件

首轮推广发生后收到第一批外部反馈，或出现第一个非维护者用户。**不再按日历排期**——时间不是这个项目的瓶颈变量。

---

## D17 · 英文化与 App Store 上架路径（分层裁决：Web 英文化放行，原生载体维持 gated，2026-07-31）

### 触发

维护者提出「计划上架苹果美区或日区应用商店，需要英文界面与英文宣传页」，并问在此之前还需要做什么。

### 核对结论（本条依据的事实，全部当日复核）

**Apple 侧（官方文本，2026-07-31 取自 developer.apple.com）**

1. **Guideline 4.2**：「Your app should include features, content, and UI that elevate it beyond a repackaged website.」4.2.2 进一步排除「web clippings, content aggregators, or a collection of links」。**纯 WKWebView 套壳是被拒第一大类**，与本项目当前形态直接冲突。
2. **Guideline 2.5.2**：「Apps should be self-contained in their bundles… nor may they download, install, or execute code which introduces or changes features or functionality of the app」。⇒ 原生壳**不得远程加载** `time.eigentime.org/app/`，运行时文件必须随包分发。这与 D8 路径 B 对「运行文件属于安装产物」的定义一致。
3. **Guideline 5.1.1(i)**：「All apps must include a link to their privacy policy in the App Store Connect metadata field **and within the app**」。⇒ 隐私政策不仅要有公开 URL，还必须在应用内可达。本仓库目前两者都没有（`site/index.html` 的 `#privacy` 是一段「数据边界」说明，不是隐私政策）。
4. **账号**：Apple Developer Program **99 USD/年**，一个账号即向 175 个国家/地区分发——**不存在「美区账号」「日区账号」之分**，美区/日区只是 Availability 勾选 + 本地化 + 税务表差异；个人（非公司）主体的**法定姓名会作为 Seller Name 公开显示**，公司名需 D-U-N-S。维护者原始设想里的「美区或日区」是一个不成立的前提，已在此更正。

**许可侧**

5. AGPL × App Store 的冲突有先例：App Store Usage Rules（设备数限制等）被 FSF 认定为对 GPL 的「附加限制」，GNU Go 与 VLC 的 iOS 移植均因此被下架。出口仍是 v31 保留的**唯一著作权人商业双许可**。当日核实：全部提交作者仅维护者两个身份（`cg`/`cgx`/`xcg`/`wowayou`，同一人），全部已合并 PR 的 author 均为 `wowayou`（AI 执行方的 PR 也在此账号下）——**双许可窗口目前仍开着**，在接受第一个外部代码贡献后关闭。

**本仓库侧（`grep` 实测，修正了初判）**

6. 「约 800 行含中文」是**高估**：含中文行里相当比例是注释（`ui.js` 196 行中 42 行是纯注释行）。真实翻译面是**非注释行里的 433 条唯一中文字面量**（`src/*.js` + `index.html`）。
7. **真正的成本大头是测试**：`tests/ui_smoke.spec.js` 有 606 行含中文定位符/断言。若直接把 UI 改成英文，等于重写整套回归。⇒ 本决策要求 **zh 保持默认、Playwright 固定 zh locale**，现有 272 条断言一字不改照常绿，英文另加薄 smoke。
8. `scripts/project_audit.py` **硬编码了两条运行时中文断言**（`aria-label="标记计划为已发生"`、`>改</button>` 的反向断言），i18n 重构必须同步改，否则 audit 会红。
9. 静态壳在 `app-ready` 之前**是可见的**：启动门闩只压 `#add-btn/.ruler/.tl-head/#timeline/.usage-day`，header 站点名与 天/周/月/年 视图切换在模块到达前就画出来了。⇒ 英文用户首访会看到一帧中文 chrome，除非 locale 在**同步内联脚本**里就解析完。这是 SPEC-013 的一个硬性设计约束，不是可选项。
10. `site/` 不进 SW 缓存（CLAUDE.md 明载）⇒ **英文 landing 与隐私政策页不需要版本仪式**；只有「应用内隐私政策入口」是运行时改动，需并入一次 bump。

### 裁决

**分三层，第二层是 gate，不是排期。**

**第一层（放行，AI 侧现在可执行）· Web 英文化**
`time.eigentime.org` 上的 PWA 加英文，与 App Store 完全解耦：零 $99、零原生壳、零许可冲突、零审核。规格 SPEC-013/014/015。这一层的产出在第三层里 100% 复用（原生壳装的就是同一套 Web 运行时），因此**不存在「先做英文是浪费」的分支**。

**第二层（gate，不是待办）· 触发条件**
以下**全部**成立前，不开原生载体项目：

- 首轮推广（runbook Phase E）已发生，且英文渠道已投；
- 出现**非维护者的真实留存用户**，且其中出现对 **Widget / 锁屏或灵动岛快捷记录 / Shortcuts** 这类 Web 做不到的能力的**反复请求**——这是 D8/ADR 0001 早已写死的触发门槛，D14 第 1 条重申「仅为上架收费不构成触发」；
- 商业双许可已自签（见下）。

D16 的判断在此完全适用且更强：**瓶颈是人肉推广步骤，不是 AI 产能**。当前外部用户 ≈ 0 时启动原生壳，是把最贵的动作排在最不确定的前提之前。

**第三层（gated）· 原生载体**
一旦触发：**必须另立独立仓库**（本仓库铁律：单页静态 / 无构建 / 无运行时依赖）。届时的形态底线由 4.2 决定——**不是「套壳 + 上架」，而是「原生能力 + 内嵌运行时」**：运行文件随包（2.5.2），并至少提供一项真实的系统集成（Widget / Live Activity / Shortcuts / 分享扩展）。前置项见 `docs/launch-runbook.md` Phase F。

### 随本条一并落定的边界

- **数据迁移断点必须写进上架文案**：WKWebView 的 `localStorage` 与网页 origin 是两个容器，现有用户（含维护者权威设备）的记录**不会自动进 App**，必须走「导出备份 → App 内导入」。不写清楚，第一个用户就丢数据。
- **App Privacy 标签是本项目最强的一张牌**：零采集、零网络、零账号——这是可核实事实，允许作为卖点；但 D3 纪律不变，英文文案同样**不得**出现 "trusted by" / "loved by" 一类未被验证的社会证明。
- **英文 App 名＝ Eigentime**（维护者当日拍板）：与已有域名 `eigentime.org` 同源、可检索、无明显撞名。catalog 里是单个 key `app.name`。**上架前仍需自行做 App Store 重名与商标检索**（这一步 AI 做不了，已登记进 runbook Phase F4）。
- **商业双许可当日自签**：`LICENSING.md` 随本决策落地——记录唯一著作权人事实、明确保留以其它条款另行授权的权利、并把贡献者入站授权补齐到「可再许可」（`CONTRIBUTING.md` 许可证节同步指向）。这关掉了 runbook F1 的时效项：此后接受的外部贡献自带第 ③ 条授权，不必事后追人。
- **v2 锁死条款不被本条推翻**：英文化是**同一产品的语言层**，不是功能扩张；SPEC-013/014/015 不得夹带任何新功能、新图表、新分类法。

### 模型路由（D15 成本治理的延续）

| 单 | 复杂度判据 | 执行 | 验收 |
|---|---|---|---|
| SPEC-013 i18n 层 | 触及全部 9 个运行时模块 + 静态壳同步脚本 + v53 快照 locale 门 + 新增 audit 护栏；一处漏改即静默回退中文 | **Opus 5** | Opus 5 |
| SPEC-014 英文 catalog | 术语表已在规格内定死，执行是机械填表 + 语言开关 UI | **Sonnet 5** | Opus 5（逐条核术语） |
| SPEC-015 英文对外页面包 | 纯静态页翻译 + 隐私政策成文，文案底稿在规格里 | **Sonnet 5** | Opus 5 |

**成本控制三条**：① 013 + 014 + 015 的应用内隐私入口**合并为一次版本仪式（v78）**（D15 已证明合并发版是最大杠杆）；② 015 的 `site/` 部分不进 SW 缓存，**零版本仪式**；③ zh 保持默认使既有 272 条回归**零改动照常绿**，避免整套测试重写。

---

## D18 · SPEC-007 解 park：主线可编辑与标签设置重设计（2026-08-01）

### 触发

维护者 v78 真机使用后提问：「标签的高级设置还是不能改、或者把某个标签设为主线，为什么不做？是忘记了还是逻辑上有问题？」

答案是两者都不是：D7（2026-07-13）登记了边界并把四类动作（改名／改桶／停用／`longOk`）拆开评估，SPEC-007 已按此写出完整裁决；随后被 D15 额度裁剪 park、D16 复盘维持 park。

### 推翻 park 的证据

D15/D16 给的 park 理由是「007 服务『已有用户的体验』，而用户数是 1」。维护者当日截图**正是这条理由的反例**：那 1 个用户的主线历史已累积 9 个标签（含 `杂` 32 条、`雖離但都是主線` 18 条、`試試` 2 条等试验残留与口语化命名），**且没有任何入口能改名或清理**。

主线标签的新增一直是可行的——录入时把桶切到「主线」并输入自定义标签即隐式 `unshift` 进 `config.mainline`（`storage.js` `addMainlineTag`），维护者的 `#app推進`(57 条) 就是这么来的。缺的从来不是「加」，是**改名／清理／显式切换当前主线**。

D7 原话要求「节点必须由真实使用证据决定」。这就是那个证据：不是「想要更好的设置页」，而是**用了 30 多天之后，隐式累积的数据没有出口**。

### 决定

1. **SPEC-007 解 park，状态转 `ready`**，执行模型 **Opus 5**（D15 认定它是队列里最大、且唯一触及数据迁移路径（主线改名）的一单）。规格正文的四类动作裁决**全部维持不变**（改名做、`longOk` 做、改桶不做、停用/删除不做，另加「设为当前主线」）。
2. **补三条 v78 之后才存在的硬约束**（写进规格 §0）：新增文案必须走 i18n catalog 且 zh/en key 对等（三条 audit 护栏会直接拦）；标签名是数据不是文案，不随语言翻译；原生 `<select>` 换分段控件这条在 v78 之后价值更高——原生弹层不跟随应用语言，英文界面下会弹中文选项。
3. **新增一项范围**：给存量 config 一个**显式**入口拿到本语言的默认标签。SPEC-014 §1.5（方案 B）「只在首次初始化种子、切语言不动数据」的规则**不改**，但它留下的真空是真实的——存量用户切英文后界面是英文、标签全中文，且无路可走。补法必须是只追加、不删除不改名不覆盖同名、追加前预览可取消，**不得**做成自动行为或切语言时弹窗。
4. **SPEC-008（landing 活体 mock）维持 park**。D16 的理由对它依然成立且未被任何新证据推翻：它服务「落地页的说服力」，而落地页访客仍然未知。**解 park 一个不等于解 park 全部。**

### 边界

- 28 天 gate 已于 D16 认定达成，本条不构成对「别镀金」条款的整体放宽。
- CLAUDE.md 的并行硬约束（10 次高质量投递 + 1 个作品集案例）不变，且 AI 侧无法核实——若求职进展落后，本单应当继续等，**这是维护者的判断，不是 AI 的**。
- 首轮推广（runbook Phase E）仍然未开始，仍然是这个项目的真实瓶颈（D16 的判断未被推翻）。本条解 park 的是一个**服务既有用户的修复类工作**，不是对外扩张。
