# Exported time-log data stays private.
*.json
!manifest.webmanifest
!package.json
!package-lock.json
!tsconfig.check.json
timelog-*.json

# Screenshots and local review artifacts can expose real records.
*.png
!icons/*.png
!docs/assets/*.png
*.jpg
*.jpeg
*.webp
*.gif
*.heic
*.avif
*.mp4
*.mov
screenshots/
ui_snapshots/
bug-screenrecordings/

# Local temp and OS files.
.DS_Store
Thumbs.db
*.tmp
*.temp
*.bak
*.swp
*.swo
.idea/
.vscode/
node_modules/
test-results/
playwright-report/
__pycache__/

# Local toolkit notes (business planning, private).
toolkit/

# Agent session notes (budget/handoff info, private).
memory/

# Design handoff bundles are external inputs, not runtime files.
*.zip

# 外部输入收件箱（截图、外部交接文档），含个人线索，永不入库。
# 通配同时覆盖尾部带空格等变体：2026-07-26 曾出现真名为「外部 /」的目录，
# 而 gitignore 会吃掉行尾空格，`外部/` 匹配不到它——规则在、却从不生效，
# 比没有规则更危险（每次 git status 都看见，每次都当「已知未跟踪」放过去）。
# 2026-07-27 已把该目录实际重命名为不带空格（此前一次「以为改了」并未生效——
# 尾部空格在文件管理器里不可见）；通配保留，防同类复发。
外部/
外部*/
