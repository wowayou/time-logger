# Exported time-log data stays private.
*.json
!manifest.webmanifest
!package.json
!package-lock.json
!tsconfig.check.json
timelog-*.json

# Screenshots and local review artifacts can expose real records.
*.png
!icons/*.png
!docs/assets/*.png
*.jpg
*.jpeg
*.webp
*.gif
*.heic
*.avif
*.mp4
*.mov
screenshots/
ui_snapshots/
bug-screenrecordings/

# Local temp and OS files.
.DS_Store
Thumbs.db
*.tmp
*.temp
*.bak
*.swp
*.swo
.idea/
.vscode/
node_modules/
test-results/
playwright-report/
__pycache__/

# Local toolkit notes (business planning, private).
toolkit/

# Agent session notes (budget/handoff info, private).
memory/

# Design handoff bundles are external inputs, not runtime files.
*.zip

# 外部输入收件箱（截图、外部交接文档），含个人线索，永不入库。
外部/
# 本机那个目录真名尾部带一个空格（`外部 /`），上面那条匹配不到它——
# gitignore 会吃掉行尾空格，必须转义。2026-07-26 发现时它已裸奔多轮。
外部\ /
外部*/
