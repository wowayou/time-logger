# 使用与理念

时间尺（Eigentime）帮助你看清一天的时间去向，判断当前主线实际推进了多少。主线可以是工作、学习、求职或其它个人目标；新安装的默认标签是「当前主线」，按自己的需要改名即可。

本页解释用法；当前进度见 [STATUS.md](STATUS.md)，开发边界与功能评审门槛见 [CLAUDE.md](CLAUDE.md) 和 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 和「屏幕使用时间」的区别

自动统计回答「设备被用了多久」，时间尺回答「你的一天去了哪里」：

- **覆盖屏幕之外**：睡觉、通勤、交谈、纸质阅读也能记录；说不清去向的时间显式显示为「未记录」。
- **由你判断含义**：同一个应用可能承载主线，也可能让你偏航，设备统计无法替你归类。
- **记录就是一次回看**：用一句话说明刚才做了什么，而不必记得先按开始。
- **数据留在本机**：无账号、不上传，完整备份可导出为 JSON。

## 记一条与补录

点右下角「＋ 记一条」，确认开始时间，写做了什么，选桶和标签，再用表单右上角按钮保存。空日默认从 00:00 开始；尾部未记录或今天的进行中段默认续该点；历史日尾部是真实记录时，从其后一分钟补记。起点仍可按实际情况调整。

点时间轴任意一行可编辑内容、标签和完整起止时间。未记录行点「补一下」补录；要在已有段内改记一段，先打开编辑页，再点「切一刀」。切分只能在原段边界内进行，不吞掉其它记录。触屏左滑一行可露出编辑和删除；删除前会预览结果，删除后最多 8 秒可撤销，下一次其它交互或跨标签页修改会结束撤销。

页面停在昨天且尾部是未记录时，普通新增会给出过夜续记选择：延续到今天，或只记到昨天 24:00。保存前核对起点、终点与预览；更早的历史日仍按自然日编辑。

窄屏使用日期时间滚轮，聚焦后也可用方向键；宽屏使用日历与时分控件。精确输入接受完整日期时间，如 `YYYY-MM-DD HH:mm`、`2026/6/28 9:5`，只写 `9:30` 不会保存，因为缺少日期。

## 四个桶与标签

| 桶 | 含义与中文默认标签 |
|---|---|
| 主线 | 当前阶段要推进的事情；默认「当前主线」 |
| 维持 | 必要消耗；睡觉、吃饭、洗漱、通勤、家务、运动健康 |
| 偏航 | 偏离当前主线的时间；娱乐、刷手机、发呆 |
| 未记录 | 没有记录覆盖的时间、空占位、未知或本机配置无法识别的标签 |

**偏航不等于错误，适时放空是必要的。** 如果你认为某项活动属于必要消耗，可在标签高级设置里把它改到维持，历史统计会按当前配置重算。

桶固定为四个，标签可以自定义。新建记录时，自定义标签首次使用会记住当时所选的桶；已有同名标签保持原来的归类。中英文切换只改变界面文案，不翻译已有标签；新安装的默认标签按首次使用语言初始化。

「··· → 标签高级设置」可新建、改名、调整维持/偏航归属、设为当前主线和设置超长段免确认。改名会迁移历史记录；只有零记录标签可以删除。新建与删除在保存前可撤销，取消则放弃整单暂存。「添加本语言的默认标签」只追加缺失标签，不覆盖已有配置。

同名合并会先展示记录去向，可选「先不合并」退出；确认合并后才改写标签。调整标签配置前宜先存一份备份。

## 看时间分布

日视图展示主线、维持、偏航和未记录的时长与比例；今天有正在进行的活动时，顶部显示「正在做」及已持续时长，点击可编辑。周/月/年视图只读汇总并可下钻；「更多 → 时间拨号盘」提供周期对比和趋势，记录不足时会明确提示。

分钟数是统计依据，百分比只用于展示，不会反向参与计算或强行凑满 100%。计划记录不计入已发生统计。

**长段待核默认关闭。** 在「更多 → 高级」开启后，超过 3 小时且未设免确认的明确标签段会附加提醒，但始终按原桶统计。确认只清除提醒；起止边界变化会使确认失效。进行中的长段要等下一条记录关闭后才能确认。该开关是本机偏好，导入备份不会替你开启。

## 外观与语言

「···」更多菜单可切换自动/亮色/暗色主题和中英文，选择跨重启保留。日视图的阶段格言可点开编辑，清空即隐藏，也可从更多菜单重新设置。

鼠标悬停图标按钮约 800ms 后显示提示，移开立即隐藏；键盘聚焦图标按钮立即显示提示。触屏不依赖悬停操作。

## 摘要、备份与迁移

「更多 → 复制当前视图摘要」复制本期 Markdown。完整历史备份在「更多 → 备份与导入」：

- **复制 JSON**：复制完整备份到剪贴板。
- **存储备份**：生成 JSON 文件；iPhone/iPad 优先系统文件面板，其它浏览器使用下载。
- **分享备份**：优先分享文件，再退化为文本或下载；取消分享不会暗中下载。
- **导入备份**：先预检。新增记录随导入一次加入，已存在记录跳过；冲突逐条选择保留本机、使用备份或合并文字，全部处理并复核最新本机数据后才写入。

「完整备份」包含当前记录与标签配置；**导入是安全合并，不是整体替换或版本回退**。它不删除备份中不存在的本机记录。清理站点数据、设备损坏或系统回收存储都可能导致数据丢失，应定期把 JSON 保存到自己能确认的位置；不要把真实备份或截图提交到公开仓库。

应用地址是 **https://time.eigentime.org/app/**。旧地址 `wowayou.github.io/time-logger/` 只读，可用于查看与导出。不同设备和站点来源的存储独立，没有自动同步；迁移需从原设备导出，再在目标设备导入。独立安卓壳与浏览器 PWA 同样不共享数据，发布进度见 [STATUS.md](STATUS.md)。

## 离线与安装

首次联网打开并缓存运行时文件后，断网也能记录、编辑、统计和备份；首次加载、检查更新和打开外部链接需要网络。浏览器缓存不等于永久安装，不能代替数据备份。

iOS 用 Safari 的「分享 → 添加到主屏幕」；Android 用 Chrome 的安装或添加到主屏幕入口。浏览器是否提供安装入口取决于访问环境。

应用没有账号、云同步、提醒推送或内置 AI；若需要复盘，可自行复制摘要交给其它工具。所有功能免费，支持作者完全自愿，不购买功能或产品方向。
